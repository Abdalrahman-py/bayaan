# No environment variables are required to run the app or backend right now.
# The recitation engine's URL has a working default baked into the backend.
# Add a key here (with an empty value) if a future change needs one.
